# GitHub Actions Workflows

Этот директорий содержит GitHub Actions workflows для автоматической сборки, тестирования и релизов.

## 📁 Структура

```
.github/
  workflows/
    build-linux.yml      # Сборка Linux AppImage
    build-windows.yml    # Сборка Windows EXE + MSI
    lint.yml            # Проверка кода (TypeScript, Rust)
    pr-checks.yml       # Проверки на Pull Requests
    release.yml         # Создание релизов
  CI-CD.md              # Полная документация CI/CD
```

## 🚀 Основные Workflows

### build-linux.yml
Собирает Linux AppImage при push на основные ветки или теги.

**Триггеры:**
- Push на `master`, `main`, `develop`
- Теги вида `v*` (для релизов)
- Pull Requests

**Выход:**
- AppImage артефакт (GitHub Actions, 30 дней)
- GitHub Release с AppImage (на тег)

### build-windows.yml
Собирает Windows EXE и MSI инсталлятор при push на основные ветки или теги.

**Триггеры:**
- Push на `master`, `main`, `develop`
- Теги вида `v*` (для релизов)
- Pull Requests

**Выход:**
- EXE артефакт (GitHub Actions, 30 дней)
- MSI инсталлятор артефакт (GitHub Actions, 30 дней)
- GitHub Release с EXE и MSI (на тег)

**Среда:**
- `windows-latest` (MSVC компилятор + link.exe уже установлены)
- Node.js 20 LTS
- Rust stable

### lint.yml
Проверяет код качество.

**Триггеры:**
- Push на основные ветки
- Pull Requests

**Проверки:**
- ✅ TypeScript/Svelte типы
- ✅ Rust clippy анализ
- ✅ Код форматирование
- ✅ Компиляция

### pr-checks.yml
Полные проверки при Pull Request.

**Триггеры:**
- Только на Pull Requests

**Проверки:**
- ✅ Все из lint.yml
- ✅ Сборка фронтенда
- ✅ Детальная информация

### release.yml
Создает GitHub Release с описанием.

**Триггеры:**
- Только на теги `v*`

**Выход:**
- GitHub Release с AppImage
- Автоматический CHANGELOG (опционально)

## 📖 Подробная документация

Полная документация по CI/CD находится в **[CI-CD.md](./CI-CD.md)**

Включает:
-详细 описание каждого workflow
- Примеры использования
- Troubleshooting
- Времена сборки
- Кеширование

## 🔧 Быстрый старт

### 1. Обычный коммит (автоматическая сборка Linux)
```bash
git push origin master
# → Запустится build-linux + lint
```

### 2. Создать релиз
```bash
git tag v0.1.0
git push origin v0.1.0
# → Запустится build-linux + release
```

### 3. Pull Request
```bash
git push origin feature-branch
# → Автоматически запустится pr-checks
```

## 📊 Статусы Workflows

Смотрите статусы на странице Actions:
https://github.com/CubicArnament/tauri-currency-converter/actions

## ⚙️ Настройка

Workflows используют следующие GitHub Actions:
- `actions/checkout@v4` - клонирование кода
- `actions/setup-node@v4` - установка Node.js
- `dtolnay/rust-toolchain@stable` - установка Rust
- `swatinem/rust-cache@v2` - кеширование Rust
- `actions/upload-artifact@v4` - загрузка артефактов
- `softprops/action-gh-release@v1` - создание релизов

Все используют официальные/проверенные версии.

## 📝 Лиценция

MIT

---

Более подробную информацию смотрите в **[CI-CD.md](./CI-CD.md)**
